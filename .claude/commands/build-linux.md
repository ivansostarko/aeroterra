Run the fastest smoke-test build to verify the project still compiles after a change:

```bash
./scripts/build-all.sh linux
```

Report the tail of `Builds/log-Linux.txt` if the build fails, and identify which recent change likely caused it.
